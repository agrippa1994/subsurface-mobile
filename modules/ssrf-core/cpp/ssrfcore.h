// AI-generated (Claude)
// Public C++ surface of the ssrf-core native module.
//
// Task 02 keeps this deliberately trivial: it only proves that C++ compiles,
// links and executes through the Expo module boundary. Task 05 replaces the
// body of this header with the real API (JSON in / JSON out) backed by the
// vendored Subsurface core.
#pragma once

namespace ssrf {

// Trivial smoke-test function. Executed in native C++.
int add(int a, int b);

} // namespace ssrf
