// AI-generated (Claude)
//
// Stand-in for Qt's <QtGlobal>, on the shim include path.
//
// Four core translation units (gas.cpp, tag.cpp, taxonomy.cpp, time.cpp) pull
// in Qt for exactly one thing: the QT_TRANSLATE_NOOP macro that marks literals
// for the translation extractor. Providing the macro from a header of the same
// name keeps those files byte-identical to upstream - no patch, no drift.
#pragma once

// Marks a string for translation without translating it. Identical semantics
// to Qt's: the macro expands to the source literal.
#ifndef QT_TRANSLATE_NOOP
#define QT_TRANSLATE_NOOP(context, sourceText) (sourceText)
#endif

#ifndef QT_TRANSLATE_NOOP3
#define QT_TRANSLATE_NOOP3(context, sourceText, comment) (sourceText)
#endif

#ifndef QT_TR_NOOP
#define QT_TR_NOOP(sourceText) (sourceText)
#endif
