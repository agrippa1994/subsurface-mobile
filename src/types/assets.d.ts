// AI-generated (Claude)
// Metro treats *.ssrf as a binary asset (see metro.config.js); requiring one
// yields the numeric module handle expo-asset resolves to a local file.
declare module '*.ssrf' {
  const asset: number;
  export default asset;
}

declare module '*.db' {
  const asset: number;
  export default asset;
}
