// AI-generated (Claude)
// Stack of the Sites tab. Site editing is task 10.
import { Stack } from 'expo-router';

export default function SitesLayout() {
  return (
    <Stack screenOptions={{ headerLargeTitle: true }}>
      <Stack.Screen name="index" options={{ title: 'Sites' }} />
    </Stack>
  );
}
