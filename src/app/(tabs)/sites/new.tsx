// AI-generated (Claude)
// Route for creating a dive site. uuid 0 means "does not exist yet".
import { SiteEditor } from '@/features/sites/site-editor';

export default function NewSiteScreen() {
  return <SiteEditor uuid={0} />;
}
